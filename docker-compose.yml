version: "3.7"

services:
  bankGarupa:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=docker
      - POSTGRESQL_PASSWORD=password
      - POSTGRESQL_DATABASE=bankGarupa
